# Provisional, non-release, read-only structural-shadow candidate. Do not
# publish before Vqro 0.9.0 or the coordinated authoritative host gate passes.
manifest_version = 2
package_contract = "vqro.package.v1"
id = "vqro.collections"
version = "0.0.0"
min_vqro_version = "0.9.0"
namespaces = ["vqro.collections"]

[[services]]
id = "collections"
provides = ["host_document"]
capabilities = ["host.terminals.read"]

[services.runtime]
kind = "component"
component = "services/collections.wasm"
world = "vqro:extension/service@1.0.0"
